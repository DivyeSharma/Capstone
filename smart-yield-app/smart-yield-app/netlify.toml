[build]
  base = "smart-yield-app"
  publish = "smart-yield-app/build"
  command = "npm install && npm run build"

[functions]
  directory = "netlify-functions"
  timeout = 10

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
